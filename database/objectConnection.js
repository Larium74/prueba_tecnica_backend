import { HOST_MYSQL, DBNAME_MYSQL, USER_MYSQL, PASSWORD_MYSQL, PORT_MYSQL} from "../config.js";

export const objectConnection = {
    host: HOST_MYSQL,
    database: DBNAME_MYSQL,
    user: USER_MYSQL,
    password: PASSWORD_MYSQL,
    port: PORT_MYSQL
}